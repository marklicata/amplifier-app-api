# Encryption Flow Diagrams

## Overview

Visual representation of how API keys flow through the system with encryption.

---

## Flow 1: Create Config (App-Side Encryption - RECOMMENDED)

```
┌──────────────────────────────────────────────────────────────────┐
│  APP (CLI/Web/Desktop)                                            │
│                                                                    │
│  1. User enters API key: "sk-ant-abc123"                          │
│                                                                    │
│  2. App encrypts locally:                                         │
│     encryptor.encrypt("sk-ant-abc123")                            │
│     → "enc:gAAAAABl1K2N..."                                       │
│                                                                    │
│  3. Send encrypted config:                                        │
│     POST /configs                                                 │
│     {"api_key": "enc:gAAAAABl1K2N..."}                           │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS (encrypted transport)
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (FastAPI)                                            │
│                                                                    │
│  4. ConfigManager receives:                                       │
│     {"api_key": "enc:gAAAAABl1K2N..."}                           │
│                                                                    │
│  5. Detects already encrypted (has "enc:" prefix)                │
│     → Skips encryption, stores as-is                              │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  DATABASE (PostgreSQL)                                            │
│                                                                    │
│  6. Stored in configs table:                                      │
│     config_json = {"api_key": "enc:gAAAAABl1K2N..."}             │
│                                                                    │
│  ✅ ENCRYPTED AT REST                                             │
└──────────────────────────────────────────────────────────────────┘
```

---

## Flow 2: Create Config (Service-Side Encryption)

```
┌──────────────────────────────────────────────────────────────────┐
│  APP (CLI/Web/Desktop)                                            │
│                                                                    │
│  1. User enters API key: "sk-ant-abc123"                          │
│                                                                    │
│  2. Send plain text config:                                       │
│     POST /configs                                                 │
│     {"api_key": "sk-ant-abc123"}                                  │
│                                                                    │
│  ⚠️  Plain text key sent over network (HTTPS only)                │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS (encrypted transport)
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (FastAPI)                                            │
│                                                                    │
│  3. ConfigManager receives:                                       │
│     {"api_key": "sk-ant-abc123"}                                  │
│                                                                    │
│  4. Encrypts sensitive field:                                     │
│     encryptor.encrypt("sk-ant-abc123")                            │
│     → "enc:gAAAAABl1K2N..."                                       │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  DATABASE (PostgreSQL)                                            │
│                                                                    │
│  5. Stored in configs table:                                      │
│     config_json = {"api_key": "enc:gAAAAABl1K2N..."}             │
│                                                                    │
│  ✅ ENCRYPTED AT REST                                             │
└──────────────────────────────────────────────────────────────────┘
```

---

## Flow 3: Retrieve Config - Encrypted (DEFAULT - Secure)

```
┌──────────────────────────────────────────────────────────────────┐
│  APP (CLI/Web/Desktop)                                            │
│                                                                    │
│  1. Request config:                                               │
│     GET /configs/{id}?decrypt=false                               │
│     (or just GET /configs/{id} - decrypt=false is default)        │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (FastAPI)                                            │
│                                                                    │
│  2. Get config from database                                      │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  DATABASE (PostgreSQL)                                            │
│                                                                    │
│  3. Retrieve:                                                     │
│     config_json = {"api_key": "enc:gAAAAABl1K2N..."}             │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (ConfigManager)                                      │
│                                                                    │
│  4. decrypt=false parameter                                       │
│     → SKIP decryption                                             │
│     → Return as-is: {"api_key": "enc:gAAAAABl1K2N..."}           │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  APP (CLI/Web/Desktop)                                            │
│                                                                    │
│  5. Receive encrypted config:                                     │
│     {                                                              │
│       "config_data": {"api_key": "enc:gAAAAABl1K2N..."},         │
│       "encrypted": true                                            │
│     }                                                              │
│                                                                    │
│  6. Decrypt locally (if needed):                                  │
│     encryptor.decrypt("enc:gAAAAABl1K2N...")                     │
│     → "sk-ant-abc123"                                             │
│                                                                    │
│  ✅ KEY STAYS ENCRYPTED OVER NETWORK                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## Flow 4: Retrieve Config - Decrypted (ADMIN/DEBUG)

```
┌──────────────────────────────────────────────────────────────────┐
│  ADMIN/DEVELOPER (Swagger UI)                                     │
│                                                                    │
│  1. Request config with decryption:                               │
│     GET /configs/{id}?decrypt=true                                │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (FastAPI)                                            │
│                                                                    │
│  2. Get config from database                                      │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  DATABASE (PostgreSQL)                                            │
│                                                                    │
│  3. Retrieve:                                                     │
│     config_json = {"api_key": "enc:gAAAAABl1K2N..."}             │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (ConfigManager)                                      │
│                                                                    │
│  4. decrypt=true parameter                                        │
│     → DECRYPT the key                                             │
│     encryptor.decrypt("enc:gAAAAABl1K2N...")                     │
│     → "sk-ant-abc123"                                             │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  ADMIN/DEVELOPER                                                  │
│                                                                    │
│  5. Receive plain text config:                                    │
│     {                                                              │
│       "config_data": {"api_key": "sk-ant-abc123"},               │
│       "encrypted": false                                           │
│     }                                                              │
│                                                                    │
│  ⚠️  PLAIN TEXT KEY EXPOSED - USE WITH CAUTION                    │
└──────────────────────────────────────────────────────────────────┘
```

---

## Flow 5: Session Creation (Internal - Always Decrypted)

```
┌──────────────────────────────────────────────────────────────────┐
│  APP (CLI/Web/Desktop)                                            │
│                                                                    │
│  1. Start session:                                                │
│     POST /sessions                                                │
│     {"config_id": "abc-123"}                                      │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ HTTPS
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (SessionManager)                                     │
│                                                                    │
│  2. Create session from config:                                   │
│     config = await get_config(config_id, decrypt=True)            │
│                                                                    │
│  3. Get from database with DECRYPTION                             │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  DATABASE (PostgreSQL)                                            │
│                                                                    │
│  4. Retrieve:                                                     │
│     config_json = {"api_key": "enc:gAAAAABl1K2N..."}             │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  API BACKEND (ConfigManager)                                      │
│                                                                    │
│  5. decrypt=true (always for sessions)                            │
│     → DECRYPT the key                                             │
│     → "sk-ant-abc123"                                             │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  SESSION MANAGER                                                  │
│                                                                    │
│  6. Create Bundle with plain key                                  │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  PROVIDER (Anthropic/OpenAI/etc.)                                 │
│                                                                    │
│  7. Receive plain API key:                                        │
│     provider.config["api_key"] = "sk-ant-abc123"                  │
│                                                                    │
│  8. Make API calls to external service                            │
│                                                                    │
│  ✅ KEY DECRYPTED IN MEMORY, NEVER EXPOSED VIA API                │
└──────────────────────────────────────────────────────────────────┘
```

---

## Security Comparison

### ❌ OLD: Always Decrypted

```
Database        API Response        Network
[encrypted] →   [plain text]    →   [plain text over HTTPS]
                                    ⚠️  Key exposed in API
```

### ✅ NEW: Encrypted by Default

```
Database        API Response        Network
[encrypted] →   [encrypted]     →   [encrypted over HTTPS]
                                    ✅ Key stays encrypted

                                    App decrypts locally
                                    when needed
```

### 🔓 NEW: Optional Decryption

```
Database        API Response        Network
[encrypted] →   [plain text]    →   [plain text over HTTPS]
                (decrypt=true)      ⚠️  Use with caution
                                    (admin/debug only)
```

---

## Key Takeaways

### 🔐 Security Benefits

1. **Encryption at Rest**: ✅ Database stores encrypted keys
2. **Encryption in Transit**: ✅ Keys stay encrypted in API responses (by default)
3. **Decryption on Demand**: ✅ Keys only decrypted when needed
4. **Clear Indicators**: ✅ `encrypted` field shows encryption state

### 📋 Best Practices

1. **Apps should:**
   - Encrypt keys before sending (recommended)
   - Use `decrypt=false` when retrieving (default)
   - Decrypt locally when needed
   - Store encrypted configs locally

2. **Backend should:**
   - Always use `decrypt=true` for internal session creation
   - Never expose plain keys via external API (unless explicitly requested)
   - Log when `decrypt=true` is used (audit trail)

3. **Admins should:**
   - Use `decrypt=true` only for debugging/testing
   - Restrict who can use `decrypt=true` (future enhancement)
   - Monitor decryption requests (future enhancement)

### 🎯 Use Cases

| Scenario | decrypt=? | Security Level |
|----------|-----------|----------------|
| App retrieves config | `false` | 🟢 High - Keys stay encrypted |
| Session creation (internal) | `true` | 🟢 High - Keys used in memory only |
| Admin debugging | `true` | 🟡 Medium - Keys exposed in API response |
| Swagger UI testing | toggle | 🟡 Medium - For development only |

---

## Testing in Swagger UI

1. **Open Swagger**: `http://localhost:8000/docs`
2. **Create config**: POST /configs with API key
3. **Test encrypted**: GET /configs/{id} with `decrypt = false`
   - See: `"api_key": "enc:gAAAAA..."`
   - Response: `"encrypted": true`
4. **Test decrypted**: GET /configs/{id} with `decrypt = true`
   - See: `"api_key": "sk-ant-..."`
   - Response: `"encrypted": false`
5. **Compare**: Toggle between encrypted/decrypted views!

---

## Summary

The encryption system now provides:

- ✅ **Secure by default**: Keys encrypted in responses
- ✅ **Flexible**: Toggle decryption when needed
- ✅ **Transparent**: Clear indicators of encryption state
- ✅ **Compatible**: Works with existing code
- ✅ **Testable**: Easy to verify in Swagger UI

Perfect for protecting API keys while maintaining flexibility for development and debugging!
